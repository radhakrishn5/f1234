import java.sql.*;
import java.util.*;

class /{
  public static void main(Strings[]a) throw Exception{
   Connection c=DriveManager.getConnection("jdbc:my")
   Scanner s=nex Scanner(System.in);
   String u=s.next.ine();
   c.createStatement().execute("SELECT " FROM users)
 }
}
  
